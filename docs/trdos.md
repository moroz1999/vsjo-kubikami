# TR-DOS Disk Loading

## Disk Variants and File Order

All five languages are exported as regular TRD and GS TRD images.

| Variant | Physical file order |
|---|---|
| Regular | `boot.B`, `LOADER.C`, `LOADING.C`, `KUBIKAMI.C` |
| General Sound | `boot.B`, `LOADER.C`, `LOADING.C`, consecutive `GSDAT00.C`… files, `KUBIKAMI.C` |

The order is part of the loader protocol. BASIC locates only `LOADER.C` by name. All later data is read by physical track and sector without directory lookup. GS data is split into catalog files of at most `255` sectors but remains one physically contiguous loader stream.

`emptytrd` creates the image, and consecutive `savetrd` directives place the files in the required order. Each file occupies `ceil(length/256)` sectors, including padding in its final sector.

## Disk Geometry and Cursor

- One sector is `256` bytes.
- One track contains `16` sectors.
- TR-DOS variable `#5CF4` stores the current sequential read position.
- When loaded with `ld de,(#5CF4)`, `D` is the track and `E` is the sector `0..15`.

The named BASIC load of `LOADER.C` leaves `#5CF4` at the first sector after that file. Every successful raw read advances it, allowing the loader to consume the remaining files sequentially.

## `#3D13` Read Contract

`trd_loader_read_sectors` calls TR-DOS routine `#3D13` with:

| Register | Value |
|---|---|
| `HL` | Destination address |
| `DE` | Track in `D`, sector in `E` |
| `B` | Sector count |
| `C` | Function `5`, read sectors |
| `IY` | System variables at `#5C3A` |

Before the call, the loader selects IM 1. After the call it executes `di`, because TR-DOS may alter interrupt state. The game later builds its IM 2 table and selects IM 2 itself.

The sector count is one byte. The loading screen and whole game must each fit in at most `255` sectors; assembler assertions enforce this limit.

## Sector Counts

`LOADING.C` is `6912` bytes, exactly `27` sectors.

The game count is calculated in assembly:

```asm
(@end-@start+255)/256
```

The current localized game images occupy `142` sectors. The count must be recalculated from labels, not copied as a fixed value.

GS block sizes and the total GS skip are generated by `gs/prepare.php`. The current stream is `1143` sectors, represented as:

- `gs_data_skip_tracks = 71`
- `gs_data_skip_sectors = 7`

When GS is absent, the loader adds the sector remainder to `E`, converts overflow past sector `15` into a track increment, adds the generated track count to `D`, and writes the result back to `#5CF4`. This must land on the first sector of `KUBIKAMI.C`.

## Loader Memory Map

The disk loader must survive while `KUBIKAMI.C` is loaded over the main game range.

| Range/address | Use |
|---|---|
| `#4000..#5AFF` | Loading screen |
| `#5B00` | GS-detected flag |
| `#5CCB...` | Runtime BASIC program and workspace |
| `#6000...` | Localized game image; its upper limit is the runtime room buffer at `#F300` |
| `#8000` | Temporary packed GS block before the game is loaded |
| `#C000` | Temporary unpacked GS block before the game is loaded |
| `#5B00..#5DFF` | Runtime screen-attribute buffer after loader handoff |
| `#F300..#F5BF` | Runtime mutable-room buffer after loader handoff |
| `#F600..#F62E` | Regular loader during disk loading; unused after handoff |
| `#F600..#F823` | GS loader during disk loading; unused after handoff |
| `#FDFD..#FF00` | Future game IM 2 dispatch and vector area |
| `#FFFF` downward | Temporary loader stack |

The `#8000` and `#C000` buffers deliberately overlap the future game image: GS data is processed first, and `KUBIKAMI.C` overwrites those buffers afterward. The largest unpacked block is `10000` bytes, ending below `#F000`.

The loader stack may temporarily use the future IM 2 area because the vector table does not exist yet. On game entry, `startup` immediately changes SP to `#FDFD`, directly below the future IM 2 dispatch, and the runtime stack grows downward. `interrupts.init` builds the IM 2 structures later. The final loader transfer is a `JP`, so the game may reuse the BASIC and TR-DOS system-variable area for its attribute buffer without returning to ROM or TR-DOS. TAP builds use the same runtime buffer and stack addresses.

The required invariants are:

- `end <= runtime_buffers.current_room_buf`.
- `trd_loader_end <= interrupts.reserved_start`.
- `runtime_buffers.end_address <= trd_loader`.
- The packed and unpacked GS buffers must not reach `#F000`.
- The loader must not be placed inside either the range overwritten by the game read or the runtime room buffer.

## Build-Time Data Staging

`release.a80` first saves `LOADING.C` from `#4000`. For a GS image, it then reuses the same `6912`-byte assembly-time memory area to save `samples.bin` in sector-aligned chunks. Files `GSDAT00.C` onward each hold at most `255` sectors; append mode extends the current file until that limit, then the next catalog file continues at the immediately following physical sector. The loader ignores file boundaries and reads the generated block counts directly. The implementation supports up to eight GS data files.

See [TRD BASIC Loader](basic_loader.md) for the bootstrap and [General Sound](general_sound.md) for the GS data stream.
